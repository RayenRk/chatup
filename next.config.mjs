/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {hostname: "elated-narwhal-30.convex.cloud"}
        ],
    },
};

export default nextConfig;
